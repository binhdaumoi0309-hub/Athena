import { CalendarDays, HeartPulse, Hospital, Stethoscope, UserRound } from 'lucide-react';import type{BookingStepProps}from'../types';import styles from'./ReviewStep.module.css';
export function ReviewStep({data,facilities,specialties,doctors}:BookingStepProps){const facility=facilities.find((item)=>item.id===data.facilityId);const specialty=specialties.find((item)=>item.id===data.specialtyId);const doctor=doctors.find((item)=>item.id===data.doctorId);return <div className={styles.review}><p>Vui lòng kiểm tra kỹ thông tin trước khi xác nhận. Lịch chỉ được ghi nhận sau khi hệ thống trả về mã đặt khám.</p><dl><div><dt><Hospital/>Cơ sở</dt><dd>{facility?.name}</dd></div><div><dt><HeartPulse/>Chuyên khoa</dt><dd>{specialty?.name}</dd></div><div><dt><Stethoscope/>Bác sĩ</dt><dd>{doctor?`${doctor.title} ${doctor.name}`:'Bác sĩ phù hợp sớm nhất'}</dd></div><div><dt><CalendarDays/>Ngày giờ</dt><dd>{data.date} lúc {data.time}</dd></div><div><dt><UserRound/>Người khám</dt><dd>{data.patientName} · {data.patientPhone}<br/>{data.patientEmail}</dd></div></dl><label><input type="checkbox" required/> Tôi xác nhận thông tin trên là chính xác và đồng ý với quy định đặt lịch của bệnh viện.</label></div>}
